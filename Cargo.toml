cargo-features = ["strip"]

[workspace]
members = ["keyboard-rp-pico", "keyboard-core"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
strip = "symbols"  # Automatically strip symbols from the binary.
lto = true
