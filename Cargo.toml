cargo-features = ["strip"]

[workspace]
members = ["rustkbd-rp-pico", "rustkbd-core"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
strip = "symbols"  # Automatically strip symbols from the binary.
lto = true
